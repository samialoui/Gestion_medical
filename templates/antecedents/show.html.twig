{% extends 'template.html.twig' %}

{% block title %}Antecedents{% endblock %}

{% block body %}
    <h1>Antecedents</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ antecedent.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ antecedent.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ antecedent.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('antecedents_index') }}">back to list</a>

    <a href="{{ path('antecedents_edit', {'id': antecedent.id}) }}">edit</a>

    {{ include('antecedents/_delete_form.html.twig') }}
{% endblock %}
