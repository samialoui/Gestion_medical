
<form method="post" action="{{ path('rendez_vous_delete', {'id': rendez_vou.id}) }}" onsubmit="return confirm('Vous etes sur à supprimer !?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendez_vou.id) }}">
    <button class="btn">Supprimer</button>
</form>
